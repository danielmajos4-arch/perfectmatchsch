export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
  // Vite automatically provides the 'from' option when processing CSS files
  // This config ensures compatibility and prevents PostCSS warnings
}
